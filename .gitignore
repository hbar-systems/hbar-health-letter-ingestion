# Dependencies
node_modules/

# Secrets / legacy local Python env — never commit
.env
.venv/
__pycache__/
*.pyc

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor
.vscode/
.idea/
*.swp
*.swo

# dist/ is intentionally NOT ignored. The built static bundle is the
# brain-app `ui_bundle` — the install pipeline clones this repo and serves
# dist/ directly, so it must be committed. Rebuild with `npm run build`.

# Test artifacts
# (synthetic_entlassbrief.pdf is intentionally tracked — no real patient data)
